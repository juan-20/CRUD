export type userType = {
    id: number,
    name: string,
    email: string,
    nascimento: string,
    telefone: string,
    userNumber: number
}

export interface MaskType {
    textmask: string;
  }