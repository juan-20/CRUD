export type userType = {
    id?: number,
    userNumber: number
    name: string,
    email: string,
    nascimento: string,
    telefone: string,
}

export interface MaskType {
    textmask: string;
  }